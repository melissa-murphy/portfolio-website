import styled from "styled-components";

export const StyledCurrentClients = styled.div`
	div.styled-row {
		justify-content: center;
		i {
			margin: 0 1rem;
			color: ${({ theme }) => theme.primary.light};
		}
	}
`;
